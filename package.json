{
  "name": "yaml-easysort",
  "main": "./lib/yaml-sortkey",
  "version": "0.1.0",
  "description": "Sort YAML keys and validate YAML (fork with line maxWidth configurable)",
  "keywords": [
    "yaml",
    "sort",
    "validate",
    "autosort"
  ],
  "activationCommands": {
    "atom-workspace": "yaml-sortkey:validate"
  },
  "activationHooks": [
    "language-yaml:grammar-used"
  ],
  "repository": "https://github.com/elledienne/yaml-sortkeys",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "js-yaml": ">=3.6.1"
  }
}
